<%= form_for(@home_theater_info) do |f| %>
  <% if @home_theater_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_theater_info.errors.count, "error") %> prohibited this home_theater_info from being saved:</h2>

      <ul>
      <% @home_theater_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
